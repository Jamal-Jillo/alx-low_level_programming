#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 * _strlen - fucntion that returns the length of a string
 * @s: character pointer
 * return: void
 */
int _strlen(char *s)
{
	return (strlen(s));
}
