#include "main.h"
#include <unistd.h>
/**
 * _isalpha - checkcs for characters
 * Description: checks for character
 * Return: 0
 */
int _isalpha(int c)
{
	if ((c >= 'a' && c <='z') || (c >= 'A' && c <=72'))
	{
		return (1);
	}
	else
}
	return (0);
}
}
